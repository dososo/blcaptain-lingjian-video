# 12 M3 前瞻加固说明

日期: 2026-07-01

## 结论

M3-a/b/c 已在离线环境落地。本轮没有改动 mock release 禁用、release 缺 FFmpeg 硬失败、stub 视频拒绝、ffprobe 不可验证 hard failure、审批 hash+签名等既有门禁。

当前机器仍缺 FFmpeg/ffprobe 与真实 provider,所以 `V-REAL-01` 继续保持 `BLOCKED_ENV`。

## M3-a provider 输出健全性校验

新增错误码:

- `LLM_OUTPUT_TOO_THIN`: LLM provider 返回的 `scenes` 为空、scene 不是对象、`narration_text` 为空,或总有效字数低于下限。
- `TTS_OUTPUT_INVALID`: TTS provider 返回空音频、非法 base64、非数值时长,或 `duration_sec <= 0`。

实现落点:

- `providers/validation.py:8`: 定义最小 narration 字数下限。
- `providers/validation.py:11`: 校验 LLM `scenes` 与 `narration_text`。
- `providers/validation.py:30`: 校验 TTS 音频字节与正数时长。
- `providers/cli.py:110`: CLI LLM 返回后调用脚本健全性校验。
- `providers/cli.py:116`: CLI TTS 返回后校验 base64 音频与 `duration_sec`。
- `providers/openai_compatible.py:52`: OpenAI-compatible LLM 返回后调用脚本健全性校验。
- `providers/openai_compatible.py:132`: OpenAI-compatible TTS 返回后校验非空音频,并用 WAV 帧数/采样率回填真实时长;非 WAV 响应保持 1 秒兜底。
- `providers/openai_compatible.py:227`: 纯 stdlib WAV 时长解析,不依赖 FFmpeg。
- `apps/cli/lingjian_cli/main.py:200`: `lj script` 捕获 provider 执行期 `LingjianError`,输出稳定 JSON 错误。
- `apps/cli/lingjian_cli/main.py:273`: `lj voice` 捕获 provider 执行期 `LingjianError`,输出稳定 JSON 错误。

新增测试:

- `tests/test_cli_contract.py:293`: `test_cli_script_rejects_thin_real_cli_output`
- `tests/test_cli_contract.py:363`: `test_cli_voice_rejects_empty_real_cli_audio`
- `tests/test_provider_contract.py:80`: `test_openai_compatible_provider_rejects_thin_script`
- `tests/test_provider_contract.py:107`: `test_openai_compatible_tts_rejects_empty_audio`
- `tests/test_provider_contract.py:135`: `test_openai_compatible_tts_uses_wav_duration`

## M3-b 真实 TTS 时长回填

实现落点:

- `apps/cli/lingjian_cli/main.py:273`: `lj voice` 使用 provider 返回的 `duration`。
- `apps/cli/lingjian_cli/main.py:292`: 单段 `duration_sec` 写入 voice plan。
- `apps/cli/lingjian_cli/main.py:295`: `total_duration_sec` 写入 voice plan。
- `packages/core/rendering.py:68`: release 渲染从 `voice_plan.total_duration_sec` 读取时长,缺失时回落 3 秒,最低 1 秒。
- `packages/core/rendering.py:130`: FFmpeg `color` 输入时长使用 `_release_duration(project)`。

新增测试:

- `tests/test_batch2_release_export.py:260`: `test_release_duration_uses_voice_plan_total_duration`
- `tests/test_batch2_release_export.py:277`: `test_release_duration_falls_back_when_voice_duration_missing`

## M3-c preview 真实渲染 opt-in

默认行为不变: preview 继续写 `LINGJIAN_STUB_MP4` stub,保证无 FFmpeg 环境离线可跑。

新增显式开关:

```bash
uv run lj render <project> --platform douyin --language zh-CN --ratio 9:16 --real --json
uv run lj preview <project> --platform douyin --language zh-CN --ratio 9:16 --real --json
```

实现落点:

- `apps/cli/lingjian_cli/main.py:358`: `lj render` 新增 `--real`。
- `apps/cli/lingjian_cli/main.py:381`: `lj render --real` 传入 `real_preview`。
- `apps/cli/lingjian_cli/main.py:405`: `lj preview` 新增 `--real`。
- `apps/cli/lingjian_cli/main.py:415`: `lj preview --real` 传入 `real_preview`。
- `packages/core/rendering.py:162`: `render_project` 新增 `real_preview` 参数,默认 `False`。
- `packages/core/rendering.py:201`: preview 仅在 `real_preview=True` 且本机可调用 `ffmpeg` 时走真实 FFmpeg 渲染。
- `packages/core/rendering.py:205`: preview real 渲染失败时回落 stub,不硬失败。

新增测试:

- `tests/test_batch2_release_export.py:293`: `test_preview_real_without_ffmpeg_falls_back_to_stub`
- `tests/test_batch2_release_export.py:302`: `test_preview_real_uses_ffmpeg_when_available`

## 离线验收

已完成的局部红绿验证:

- 新增 8 条 M3 测试先失败,失败点为缺少 provider 校验、缺少 `real_preview` 参数与旧 TTS 错误码。
- 实现后同 8 条测试通过。

最终离线回归命令:

- `uv run python scripts/ci/run_verification.py`: 51 PASS / 1 BLOCKED_ENV / 0 FAIL,`V-REAL-01=BLOCKED_ENV`。
- `uv run pytest -q`: 65 passed。
- `uv run ruff check .`: 通过。
- `uv run python scripts/ci/check_false_success.py`: 通过,FS-01 到 FS-13 均无 findings。
- `uv run python scripts/ci/check_no_force.py`: 通过。
- `uv run python scripts/ci/check_forbidden_imports.py`: 通过。
- `uv run python scripts/ci/check_render_engine_m1.py`: 通过。
- `uv run python scripts/ci/check_ffmpeg_card_scope.py`: 通过。
- `pnpm --dir apps/web lint`: 通过。
- `pnpm --dir apps/web build`: 通过。

## 未改变的边界

- mock provider 仍不能用于 release。
- `render --release` 缺 FFmpeg/ffprobe 仍抛 `RELEASE_RENDER_REQUIRES_FFMPEG`。
- release stub 仍被 `RELEASE_VIDEO_IS_STUB` 拒绝。
- release 视频不可被 ffprobe 验证仍是 `RENDER_NOT_VERIFIABLE`。
- `V-REAL-01` 在本机无真实环境时仍必须是 `BLOCKED_ENV`。
- 未引入 yt-dlp、youtube-dl、Remotion、HyperFrames、Playwright 到 core/provider/engine 主路径。
