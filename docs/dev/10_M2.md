# M2 真实 provider/渲染落地说明

日期: 2026-07-01

## 结论

M2 代码落地已完成,离线环境回归为 51 PASS / 1 BLOCKED_ENV / 0 FAIL,pytest 为 56 passed。当前机器仍缺 FFmpeg、ffprobe、真实 LLM provider、真实 TTS provider,所以 `V-REAL-01` 仍保持诚实 `BLOCKED_ENV`;没有把 fake CLI/API 单测冒充真实 release PASS。

## 当前环境规格

- `ffmpeg`: 未找到。
- `ffprobe`: 未找到。
- provider 类型: 未配置真实 CLI/API provider。
- `V-REAL-01`: `BLOCKED_ENV`,见 `verification/evidence/V-REAL-01.log`。

## M2-1 真实 provider

- `providers/cli.py:17`: 新增 `CliProvider`,从 `LINGJIAN_LLM_CLI` / `LINGJIAN_TTS_CLI` 读取本机命令,stdin 输入 JSON,stdout 输出 JSON;`is_mock=False`。
- `providers/openai_compatible.py:16`: 新增 `OpenAICompatibleLLMProvider`;显式 opt-in 后调用 OpenAI-compatible `/chat/completions`;`is_mock=False`。
- `providers/openai_compatible.py:94`: 新增 `OpenAICompatibleTTSProvider`;显式 opt-in 后调用 OpenAI-compatible `/audio/speech`;`is_mock=False`。
- `providers/openai_compatible.py:207`: HTTP 错误接入既有 provider 错误分类,不把 key 写入错误 details。
- `providers/registry.py:9`: 注册 `llm_cli`、`tts_cli`、`openai_compatible`、`openai_compatible_tts`,并在未配置时继续返回 `PROVIDER_NOT_CONFIGURED`。
- `apps/cli/lingjian_cli/main.py:197`: `lj script --provider llm_cli/openai_compatible` 调用 provider 生成 scenes,写入 `provider_is_mock=false`。
- `apps/cli/lingjian_cli/main.py:260`: `lj voice --provider tts_cli/openai_compatible_tts` 调用 provider 写入音频与 `provider_is_mock=false`。

新增测试:

- `tests/test_provider_contract.py:27`: `test_cli_provider_resolves_from_environment`
- `tests/test_provider_contract.py:43`: `test_openai_compatible_provider_resolves_from_environment`
- `tests/test_provider_contract.py:57`: `test_openai_compatible_provider_maps_http_error`
- `tests/test_cli_contract.py:254`: `test_cli_script_uses_configured_real_cli_provider`
- `tests/test_cli_contract.py:293`: `test_cli_voice_uses_configured_real_cli_provider`
- `tests/test_cli_contract.py:327`: `test_cli_script_uses_openai_compatible_provider`
- `tests/test_cli_contract.py:380`: `test_cli_voice_uses_openai_compatible_tts_provider`

## M2-2 release ffmpeg_card 真出片

- `packages/core/rendering.py:55`: release 渲染读取脚本文案。
- `packages/core/rendering.py:68`: release 渲染读取语音时长。
- `packages/core/rendering.py:100`: 复用 `break_cjk_text`,生成 FFmpeg `drawtext` filter。
- `packages/core/rendering.py:123`: release 模式调用 FFmpeg 生成 H.264/yuv420p MP4。
- `packages/core/rendering.py:182`: release 仍保留 FFmpeg/ffprobe 硬门禁。
- `packages/core/rendering.py:192`: preview 继续写 stub;release 只在 FFmpeg 成功后写 manifest,并拒绝 stub 哨兵。

新增测试:

- `tests/test_batch2_release_export.py:219`: `test_release_render_uses_ffmpeg_and_writes_non_stub_video`

## M2-3 V-REAL-01 终验状态

- `scripts/ci/run_verification.py` 仍按 M1 整改后的逻辑先跑 doctor;doctor ready 才执行 `script -> voice -> visuals -> approve -> render --release -> qa --release -> export --release -> ffprobe`。
- 当前机器 doctor 不 ready,`verification/results.json` 中 `V-REAL-01` 仍为 `BLOCKED_ENV`。
- 需要在具备 FFmpeg/ffprobe 与真实 CLI/API provider 的环境补跑 `uv run python scripts/ci/run_verification.py`,届时 `V-REAL-01` 才能转为真实 PASS。
- 真实环境终验 runbook 与当前阻塞证据见 `docs/dev/11_REAL_VERIFY.md`。

## M2-4 ffprobe timeout

- `packages/core/qa.py:13`: 新增 `FFPROBE_TIMEOUT_SEC = 20`。
- `packages/core/qa.py:43`: ffprobe 子进程增加 timeout;`TimeoutExpired` 返回不可验证,最终进入 `RENDER_NOT_VERIFIABLE`。

新增测试:

- `tests/test_batch2_release_export.py:331`: `test_release_qa_timeout_marks_video_not_verifiable`

## license 与合规

- `packages/core/exporting.py:61`: `license_manifest.md` 登记 `llm_cli` / `tts_cli` 为 `User supplied CLI provider`,登记 `openai_compatible` / `openai_compatible_tts` 为 `OpenAI-compatible API provider`。
- `tests/test_batch2_release_export.py:75`: `test_export_license_manifest_records_cli_provider`
- `tests/test_batch2_release_export.py:96`: `test_export_license_manifest_records_openai_provider_without_secrets`
- 不记录 CLI 命令内容、base URL、model、key、环境变量值或本机私密路径到 release 包。

## 验收命令

已在当前离线环境重跑:

- `uv run python scripts/ci/run_verification.py`: 通过,`failures=0`;`V-REAL-01=BLOCKED_ENV`。
- `uv run pytest -q`: 通过,56 passed。
- `uv run ruff check .`: 通过。
- `uv run python scripts/ci/check_false_success.py`: 通过。
- `uv run python scripts/ci/check_no_force.py`: 通过。
- `uv run python scripts/ci/check_forbidden_imports.py`: 通过。
- `uv run python scripts/ci/check_render_engine_m1.py`: 通过。
- `uv run python scripts/ci/check_ffmpeg_card_scope.py`: 通过。
- `pnpm --dir apps/web lint`: 通过。
- `pnpm --dir apps/web build`: 通过。

## 真实终验补跑要求

在真实环境中提供:

```bash
export LINGJIAN_LLM_CLI=/path/to/real-llm-cli
export LINGJIAN_TTS_CLI=/path/to/real-tts-cli
uv run lj doctor --json
uv run python scripts/ci/run_verification.py
```

或使用 OpenAI-compatible API:

```bash
export OPENAI_BASE_URL=https://api.example.com/v1
export OPENAI_API_KEY=...
export OPENAI_MODEL=...
export OPENAI_TTS_BASE_URL=https://api.example.com/v1
export OPENAI_TTS_API_KEY=...
export OPENAI_TTS_MODEL=...
uv run lj doctor --json
uv run python scripts/ci/run_verification.py
```

`doctor` 必须显示 `ready=true`,且 `ffmpeg`、`ffprobe`、真实 LLM/TTS provider 均 ready。补跑后应记录 FFmpeg/ffprobe 版本、provider 类型和 `verification/evidence/V-REAL-01.log` 中的真实 ffprobe 输出。
